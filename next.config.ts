import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["avatars.githubusercontent.com", "i.pravatar.cc" , "lh3.googleusercontent.com"], // Tambahkan domain gambar
  },
};

export default nextConfig;
